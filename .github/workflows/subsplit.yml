name: subsplit

on:
  push:
    tags:
      - '*'
    branches:
      - '*'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  subsplit:
    name: Split Packages (${{ matrix.package.local_path }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          # Contracts
          - local_path: 'libs/contracts/lexer'
            split_repository: 'lexer-contracts'
          # Metadata
          - local_path: 'libs.old/meta/runtime'
            split_repository: 'runtime'
          # Documentation
          - local_path: 'docs'
            split_repository: 'docs'
          # Legacy: Kernel Libraries
          - local_path: 'libs.old/compiler'
            split_repository: 'compiler'
          - local_path: 'libs.old/buffer'
            split_repository: 'buffer'
          - local_path: 'libs.old/exception'
            split_repository: 'exception'
          - local_path: 'libs.old/lexer'
            split_repository: 'lexer'
          - local_path: 'libs.old/parser'
            split_repository: 'parser'
          - local_path: 'libs.old/position'
            split_repository: 'position'
          - local_path: 'libs.old/source'
            split_repository: 'source'
          - local_path: 'libs.old/visitor'
            split_repository: 'visitor'
          # Legacy: Contracts
          - local_path: 'libs.old/contracts/ast'
            split_repository: 'ast-contracts'
          - local_path: 'libs.old/contracts/exception'
            split_repository: 'exception-contracts'
          - local_path: 'libs.old/contracts/parser'
            split_repository: 'parser-contracts'
          - local_path: 'libs.old/contracts/position'
            split_repository: 'position-contracts'
          - local_path: 'libs.old/contracts/source'
            split_repository: 'source-contracts'
    steps:
      - uses: actions/checkout@v4

      # no tag
      - if: "!startsWith(github.ref, 'refs/tags/')"
        name: Monorepo Split of ${{ matrix.package.split_repository }}
        uses: danharrin/monorepo-split-github-action@v2.3.0
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        with:
          branch: "master"
          package_directory: '${{ matrix.package.local_path }}'
          repository_organization: 'phplrt'
          repository_name: '${{ matrix.package.split_repository }}'
          user_name: "SerafimArts"
          user_email: "nesk@xakep.ru"

      # with tag
      - if: "startsWith(github.ref, 'refs/tags/')"
        name: Monorepo Tagged Split of ${{ matrix.package }}
        uses: danharrin/monorepo-split-github-action@v2.3.0
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        with:
          tag: ${GITHUB_REF#refs/tags/}
          branch: "master"
          package_directory: '${{ matrix.package.local_path }}'
          repository_organization: 'phplrt'
          repository_name: '${{ matrix.package.split_repository }}'
          user_name: "SerafimArts"
          user_email: "nesk@xakep.ru"
