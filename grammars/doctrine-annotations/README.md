# Doctrine Annotations

The annotations read out of a doc comment by
[doctrine/annotations](https://github.com/doctrine/annotations): a name, the
arguments it is called with, and the values those are written of.

Grammar written after the EBNF in the docblocks of `DocParser.php`. The prose
of a doc comment is not part of it. Licence: MIT.
