(
    $size is positive-int
        ? non-empty-array<int, Demo\Entity\User>
        : (
            TValue is not Countable
                ? Demo\Status::ACTIVE
                : array{empty: false}
        )
)
