# Composer Version Constraint

The versions a package is allowed to be installed at, written in the `require`
section of a `composer.json`.

Grammar written after the [Composer documentation](https://getcomposer.org/doc/articles/versions.md).
Licence: MIT.
